name: Proxy Cleaner Test

on:
  workflow_dispatch:

jobs:
  test:

    runs-on: ubuntu-latest

    steps:

    - name: Checkout
      uses: actions/checkout@v4

    - name: Show system
      run: |
        echo "GitHub Actions OK"
        uname -a
